{
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "build": "yarn esbuild src/server.jsx --bundle --platform=node --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.12.24"
  }
}
